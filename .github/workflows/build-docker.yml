name: Build and Publish

on:
  push:

## Add global env vars and matrix configuration for the whole workflow
env:
  PACKAGE_NAME: "tollgate-module-basic-go"
  OPENWRT_VERSION: "24.10.1"

# Define the architecture matrix once for reuse across jobs
# This allows us to maintain architecture configurations in one place
jobs:
  define-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          # Define architecture matrix as JSON with OpenWRT version included
          echo "matrix={\"include\":[{\"architecture\":\"arm64_cortex-a53\",\"goarch\":\"arm64\",\"sdk\":\"mediatek-filogic\",\"openwrt_version\":\"24.10.1\"}]}" >> $GITHUB_OUTPUT
          # Uncomment additional architectures as needed:
          # {\"architecture\":\"amd64\",\"goarch\":\"amd64\",\"sdk\":\"x86-64\"},
          # {\"architecture\":\"arm_cortex-a7\",\"goarch\":\"arm\",\"goarm\":\"7\",\"sdk\":\"arm_cortex-a7\"},
          # {\"architecture\":\"mipsel_24kc\",\"goarch\":\"mipsle\",\"gomips\":\"softfloat\",\"sdk\":\"mipsel_24kc\"},
          # {\"architecture\":\"mips_24kc\",\"goarch\":\"mips\",\"gomips\":\"softfloat\",\"sdk\":\"mips_24kc\"}

  determine-versioning:
    runs-on: ubuntu-latest
    outputs:
      package_version: ${{ steps.determine-package-version.outputs.package_version }}
      release_channel: ${{ steps.determine-release-channel.outputs.release_channel }}
    steps:
      - id: commit-hash
        uses: prompt/actions-commit-hash@v3

      - name: Set package_version variable
        id: determine-package-version
        run: |
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            # If this is a tag push, use the tag name
            echo "package_version=${GITHUB_REF_NAME}" >> $GITHUB_OUTPUT
          else
            # If this is a branch push, use branch-shortCommitHash format
            echo "package_version=${GITHUB_REF_NAME}-${{ steps.commit-hash.outputs.short }}" >> $GITHUB_OUTPUT
          fi
        
      - name: Set version variable
        id: determine-release-channel
        run: |
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            echo "release_channel=stable" >> $GITHUB_OUTPUT
          else
            echo "release_channel=dev" >> $GITHUB_OUTPUT
          fi

  build-package:
    needs: [determine-versioning, define-matrix]
    runs-on: ubuntu-latest
    container:
      image: openwrt/sdk:${{ matrix.sdk }}-${{ matrix.openwrt_version }}
      options: --user root
    strategy:
      matrix: ${{ fromJson(needs.define-matrix.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4
        with:
          path: ${{ env.PACKAGE_NAME }}/
          fetch-depth: 0

      - name: Initialize
        run: |
          PACKAGE_FILENAME=${{ env.PACKAGE_NAME }}_${{ needs.determine-versioning.outputs.package_version }}_${{ matrix.architecture }}
          echo "PACKAGE_FILENAME=$PACKAGE_FILENAME" >> $GITHUB_ENV
          echo "ℹ️ PACKAGE_FILENAME: $PACKAGE_FILENAME"

          # Move sources to sdk folder
          mv ${{ env.PACKAGE_NAME }} /builder/package/${{ env.PACKAGE_NAME }}

      - name: Install Golang
        run: |
          # Add golang-backports repository
          echo "deb https://deb.debian.org/debian bookworm-backports main" > /etc/apt/sources.list.d/backports.list
          apt-get update
          # Install Go from backports
          apt-get install -y -t bookworm-backports golang-go

      - name: Configure SDK
        run: |
          cd /builder
          # Generate default configuration
          make defconfig
          # Update configuration for our needs
          echo "CONFIG_PACKAGE_${{ env.PACKAGE_NAME }}=y" >> .config

      - name: Compile Package
        run: |
          cd /builder

          # Only set GOMIPS for MIPS architectures (https://stackoverflow.com/q/55154143)
          if [[ "${{ matrix.goarch }}" == "mips" || "${{ matrix.goarch }}" == "mipsle" ]]; then
            GOMIPS=softfloat
          fi

          env GOARCH=${{ matrix.goarch }} \
            GOMIPS=$GOMIPS \
            PACKAGE_VERSION=${{ needs.determine-versioning.outputs.package_version }} \
            make -j$(nproc) V=sc package/${{ env.PACKAGE_NAME }}/compile \
            ${{ env.DEBUG == 'true' && 'V=sc' || '' }}

      - name: List output package(s)
        run: |
          echo "Listing all .ipk files in /builder/bin/packages:"
          find /builder/bin/packages -name "*.ipk" -type f
          PACKAGE_PATH=$(find /builder/bin/packages -name "*.ipk" -type f | head -n1)
          if [ -z "$PACKAGE_PATH" ]; then
            echo "❌ No .ipk package found in /builder/bin/packages"
            exit 1
          fi
          echo "✅ Found package at: $PACKAGE_PATH"
          # Copy file to a location GitHub can access
          mkdir -p /github/workspace/artifacts
          cp "$PACKAGE_PATH" "/github/workspace/artifacts/${{ env.PACKAGE_FILENAME }}"

      - name: GitHub Artifact upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.PACKAGE_FILENAME }}
          path: /github/workspace/artifacts/${{ env.PACKAGE_FILENAME }}
          retention-days: 5

  publish-metadata:
    needs: [build-package, define-matrix]
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.define-matrix.outputs.matrix) }}
    steps:
      

      - name: Initialize
        id: init
        run: |
          PACKAGE_FILENAME=${{ env.PACKAGE_NAME }}_${{ needs.determine-versioning.outputs.package_version }}_${{ matrix.architecture }}
          echo "PACKAGE_FILENAME=$PACKAGE_FILENAME" >> $GITHUB_ENV
          echo "ℹ️ PACKAGE_FILENAME: $PACKAGE_FILENAME"

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.PACKAGE_NAME }}_${{ matrix.architecture }}
          path: ./artifacts

      - name: Prepare package metadata
        id: package_info
        run: |
          PACKAGE_PATH=$(find ./artifacts -name "${{ env.PACKAGE_FILENAME }}" -type f | head -n1)
          echo "ℹ️ PACKAGE_PATH: ${{ env.PACKAGE_PATH }}"

          # Calculate file size
          FILE_SIZE=$(stat -c%s "$PACKAGE_PATH")
          echo "PACKAGE_PATH=$PACKAGE_PATH" >> $GITHUB_ENV
          echo "PACKAGE_SIZE=$FILE_SIZE" >> $GITHUB_ENV

      - name: Blossom Artifact upload
        id: blossom_upload
        uses: Origami74/upload-blossom-action@improve-io
        with:
          host: "https://blossom.swissdash.site"
          filePath: ${{ env.PACKAGE_PATH }}

      - name: Process Blossom response and extract hash
        run: |
          echo "Upload outputs:"
          echo "  URL: ${{ steps.blossom_upload.outputs.url }}"
          echo "  Hash: ${{ steps.blossom_upload.outputs.hash }}"
          echo "  Size: ${{ env.PACKAGE_SIZE }} bytes"
          # Extract hash from URL
          URL="${{ steps.blossom_upload.outputs.url }}"
          # Use basename to extract filename from URL
          FILENAME=$(basename "$URL")
          # Remove the file extension to get the hash
          HASH="${FILENAME%.ipk}"
          echo "Extracted hash from URL: $HASH"
          # Store values in environment variables
          echo "PACKAGE_URL=$URL" >> $GITHUB_ENV
          echo "PACKAGE_HASH=$HASH" >> $GITHUB_ENV
          # Verify hash was extracted successfully
          if [ -z "$HASH" ]; then
            echo "❌ ERROR: Failed to extract hash from URL"
            exit 1
          fi
          # Verify hash is a valid hex string (64 characters for SHA-256)
          if ! [[ $HASH =~ ^[0-9a-f]{64}$ ]]; then
            echo "❌ ERROR: Extracted hash doesn't appear to be a valid SHA-256 hash"
            exit 1
          fi
          echo "✅ Successfully extracted hash: $HASH"

      - name: Publish Package NIP-94 Metadata
        id: publish_package
        uses: OpenTollGate/nostr-publish-file-metadata-action@v0.1.0
        with:
          relays: >-
            wss://relay.damus.io,
            wss://nos.lol,
            wss://nostr.mom/
          url: ${{ env.PACKAGE_URL }}
          mimeType: application/octet-stream
          fileHash: ${{ env.PACKAGE_HASH }}
          originalHash: ${{ env.PACKAGE_HASH }}
          filename: "${{ env.PACKAGE_FILENAME }}"
          content: "${{ env.PACKAGE_CONTENT }}"
          nsec: ${{ secrets.NSEC_HEX }}
          size: ${{ env.PACKAGE_SIZE }}
          customTags: |
            architecture=${{ env.MODEL }}
            version=${{ needs.determine-versioning.outputs.package_version }}
            release_channel=${{ needs.determine-versioning.outputs.release_channel }}

      # Debug the outputs to see what's available
      - name: Debug Event ID
        run: |
          echo "EVENT_ID from env: ${{ env.EVENT_ID }}"
          echo "NOTE_ID from env: ${{ env.NOTE_ID }}"
          echo "Output from publish step: ${{ steps.publish_package.outputs.eventId || 'not set' }}"
          echo "Outputs available:"
          echo "${{ toJSON(steps.publish_package.outputs) }}"

      - name: Verify Package NIP-94 Event
        id: verify_package
        # Use environment variable instead of step output
        if: env.EVENT_ID != ''
        uses: OpenTollGate/nostr-publish-file-metadata-action/python/verify@v0.1.0
        with:
          eventId: ${{ env.EVENT_ID }}
          relays: >-
            wss://relay.damus.io,
            wss://nos.lol,
            wss://nostr.mom/

      - name: Check Package Publication
        # Only run this step if the verification ran and failed
        if: steps.verify_package.outcome == 'failure'
        run: |
          echo "❌ Failed to verify package publication on relays"
          exit 1

      # Add a step to handle the case where EVENT_ID is empty
      - name: Check Event ID
        if: env.EVENT_ID == ''
        run: |
          echo "❌ No event ID was generated by the publish step"
          exit 1

      - name: Build Summary
        run: |
          echo "Build Summary for ${{ matrix.architecture }}:"
          echo "✅ Binary successfully built and published"
          echo "✅ Package successfully built and published"
          echo "Binary URL: ${{ steps.upload_binary.outputs.blossomUrl }}"
          echo "Package URL: ${{ steps.upload_package.outputs.blossomUrl }}"
  trigger-build-os:
    name: Trigger new build TollGate OS
    runs-on: ubuntu-latest
    needs: publish-metadata
    steps:
    - name: Trigger OS Workflow
      uses: peter-evans/repository-dispatch@v2
      with:
        token: ${{ secrets.REPO_ACCESS_TOKEN }}
        repository: OpenTollGate/tollgate-os
        event-type: update-release-json