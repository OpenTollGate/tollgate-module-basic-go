# Editor / OS
*.DS_Store
.idea
.roo/

# Merge / patch artifacts
*.orig

# Go build outputs (binaries that land at repo or src/ root)
bin/
artifacts/
vendor/
tollgate
tollgate-wrt
tollgate-module-basic-go
tollgate-cli
*.exe

# Test artifacts
**/tollwallet_db/*
tests/*.bin
tests/__pycache__/
tests/.env

# Router credentials (copy from routers.env.example)
routers.env

# Router mutex (shared lock file at /root/routers.lock)
routers.lock

# AI tooling — shared skills are symlinked, local memory is per-repo
.claude/
CLAUDE.md
agents/
skills
memory-global
memory-project/
reference/
.opencode/

# LLM planning documents — not for release
*-plan.md
PLAN-*.md
TODO-*.md
MOCK-*.md
TEST-COVERAGE-*.md
PR*-DECOMPOSITION-*.md
docs/tmp/